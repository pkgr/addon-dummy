#!/bin/bash

set -ex

echo $APP_NAME
echo $APP_USER
echo $APP_GROUP
echo $APP_VERSION
echo $APP_ITERATION
echo $APP_SAFE_NAME

ROOT_DIR=$(dirname $(dirname $0))

find ${ROOT_DIR}/debian -type f -print0 | xargs -0 sed -i "s/_APP_NAME_/${APP_NAME}/g"
